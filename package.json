{
  "scripts": {
    "test": "percy exec -v -- mvn clean test -P bstack-single -U -Dwdm.cachePath=./seleniumCache -Dwdm.resolutionCachePath=./resolutionCache"
  },
  "devDependencies": {
    "@percy/cli": "^1.27.3"
  }
}
